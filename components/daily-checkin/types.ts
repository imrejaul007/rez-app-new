/**
 * Shared types for daily-checkin sub-components
 */
import type {
  CheckInReward,
  StreakBonus,
  PromotionalPoster,
  ShareSubmission,
  AffiliateStats,
} from '@/services/gamificationApi';

export type { CheckInReward, StreakBonus, PromotionalPoster, ShareSubmission, AffiliateStats };
