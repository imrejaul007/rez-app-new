# Phase 3.3: Visual Overview & Architecture

## 🏗️ Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      MainStorePage                          │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │         SafeAreaContainer                           │   │
│  │  (handles device notches & safe areas)              │   │
│  │                                                      │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  State Management                             │  │   │
│  │  │  - Loading: <LoadingSpinner />                │  │   │
│  │  │  - Error: <ErrorState />                      │  │   │
│  │  │  - Empty: <EmptyProducts />                   │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                      │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  ResponsiveProductGrid                        │  │   │
│  │  │  ├─ Auto-calculates columns                   │  │   │
│  │  │  ├─ FlatList optimized                        │  │   │
│  │  │  └─ Responsive to screen size                 │  │   │
│  │  │                                                │  │   │
│  │  │  [Product] [Product] [Product]                │  │   │
│  │  │  [Product] [Product] [Product]                │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                      │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │  BottomSheet (Filters)                        │  │   │
│  │  │  └─ Slides up from bottom                     │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 State Flow Diagram

```
                    ┌──────────────┐
                    │  Page Loads  │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │   Loading?   │
                    └──────┬───────┘
                           │
                    ┌──────┴───────┐
                    │              │
                   YES             NO
                    │              │
                    ▼              ▼
          ┌──────────────┐   ┌──────────────┐
          │ LoadingState │   │    Error?    │
          └──────────────┘   └──────┬───────┘
                                    │
                             ┌──────┴───────┐
                             │              │
                            YES             NO
                             │              │
                             ▼              ▼
                    ┌──────────────┐   ┌──────────────┐
                    │  ErrorState  │   │   Empty?     │
                    │  + Retry     │   └──────┬───────┘
                    └──────────────┘          │
                                       ┌──────┴───────┐
                                       │              │
                                      YES             NO
                                       │              │
                                       ▼              ▼
                              ┌──────────────┐   ┌──────────────┐
                              │EmptyProducts │   │Product Grid  │
                              │+ Clear Filter│   │  Display     │
                              └──────────────┘   └──────────────┘
```

---

## 🎨 Component Relationships

```
┌───────────────────────────────────────────────────────────────┐
│                     State Components                          │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  EmptyState (Base)                                            │
│  ├── EmptyProducts (Specialized)                              │
│  ├── EmptyOrders (Future)                                     │
│  └── EmptyWishlist (Future)                                   │
│                                                               │
│  ErrorState                                                   │
│  └── Handles all error scenarios                              │
│                                                               │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│                    Mobile Components                          │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  SafeAreaContainer                                            │
│  ├── Wraps entire pages                                       │
│  └── Configurable edges                                       │
│                                                               │
│  BottomSheet                                                  │
│  ├── Filters                                                  │
│  ├── Options menus                                            │
│  └── Mobile-friendly modals                                   │
│                                                               │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│                    Product Components                         │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  ResponsiveProductGrid                                        │
│  ├── Uses useResponsiveGrid hook                              │
│  ├── FlatList optimization                                    │
│  └── Renders ProductCard components                           │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

---

## 📱 Responsive Behavior

```
Screen Size: XS (Mobile)          SM (Mobile Landscape)      MD (Tablet)
Width:       320px - 575px        576px - 767px              768px - 991px
Columns:     2                    2                          3

┌─────────┬─────────┐            ┌─────────┬─────────┐       ┌────┬────┬────┐
│ Product │ Product │            │ Product │ Product │       │ P  │ P  │ P  │
├─────────┼─────────┤            ├─────────┼─────────┤       ├────┼────┼────┤
│ Product │ Product │            │ Product │ Product │       │ P  │ P  │ P  │
└─────────┴─────────┘            └─────────┴─────────┘       └────┴────┴────┘


Screen Size: LG (Desktop)         XL (Large Desktop)         XXL (Extra Large)
Width:       992px - 1199px       1200px - 1399px            1400px+
Columns:     4                    4                          4

┌───┬───┬───┬───┐                ┌───┬───┬───┬───┐          ┌───┬───┬───┬───┐
│ P │ P │ P │ P │                │ P │ P │ P │ P │          │ P │ P │ P │ P │
├───┼───┼───┼───┤                ├───┼───┼───┼───┤          ├───┼───┼───┼───┤
│ P │ P │ P │ P │                │ P │ P │ P │ P │          │ P │ P │ P │ P │
└───┴───┴───┴───┘                └───┴───┴───┴───┘          └───┴───┴───┴───┘
```

---

## 🎯 Empty State Variations

```
┌─────────────────────────────────────────────────────────────┐
│                    EmptyProducts                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  No Filters Active:                                         │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                     🔍                                 │  │
│  │           "No Products Found"                          │  │
│  │   "This store doesn't have any products yet."         │  │
│  │       "Check back soon!"                               │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  Filters Active:                                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                     🔍                                 │  │
│  │           "No Products Found"                          │  │
│  │  "We couldn't find any products matching your         │  │
│  │   filters. Try adjusting them."                        │  │
│  │                                                         │  │
│  │            [ Clear Filters ]                           │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## ⚙️ Hook Usage Flow

```
useResponsiveGrid Hook Flow:

1. Component Mounts
   │
   ▼
2. Get Screen Dimensions
   │
   ▼
3. Calculate Based on Breakpoints
   ├─ XS (0px):    2 columns
   ├─ SM (576px):  2 columns
   ├─ MD (768px):  3 columns
   ├─ LG (992px):  4 columns
   └─ XL (1200px): 4 columns
   │
   ▼
4. Calculate Card Width
   ├─ Total Gap = gap × (columns + 1)
   ├─ Available Width = Screen Width - Total Gap
   └─ Card Width = Available Width / Columns
   │
   ▼
5. Return Config
   └─ { numColumns, cardWidth, gap }
   │
   ▼
6. Listen for Screen Changes
   └─ Dimensions.addEventListener('change')
   │
   ▼
7. Recalculate on Change
   └─ Loop back to step 2
```

---

## 🔄 BottomSheet Animation

```
State: Hidden                      State: Animating                State: Visible

┌──────────────┐                  ┌──────────────┐                ┌──────────────┐
│              │                  │              │                │   ┌──────┐   │
│   Content    │                  │   Content    │                │   │ ──── │   │
│              │                  │              │  ▲            │   │Title │   │
│              │                  │              │  │            │   ├──────┤   │
└──────────────┘                  │  ┌────────┐  │  │            │   │Filter│   │
                                   │  │  ──── │  │  │            │   │Form  │   │
[Sheet Below Screen]              │  │ Sheet │  │  │ Slide       │   │      │   │
                                   │  │       │  │  │ Up         │   │      │   │
                                   │  └───────┘  │  │            │   └──────┘   │
                                   └─────────────┘               └──────────────┘
```

---

## 📏 Safe Area Handling

```
Device with Notch:

┌─────────────────────────────┐
│░░░░░░░░░[NOTCH]░░░░░░░░░░░░░│ ← Top Inset
├─────────────────────────────┤
│                             │
│        Safe Content         │
│         Area Here           │
│                             │
├─────────────────────────────┤
│░░░░░░░░░░░░░░░░░░░░░░░░░░░░│ ← Bottom Inset (Home Indicator)
└─────────────────────────────┘

SafeAreaContainer applies padding:
- paddingTop: insets.top
- paddingBottom: insets.bottom
- paddingLeft: insets.left (if needed)
- paddingRight: insets.right (if needed)
```

---

## 🎨 Design Token Usage

```
Component Styling Hierarchy:

DesignTokens.ts (Source of Truth)
├── SPACING
│   └── Used by: All components for consistent padding/margins
├── TYPOGRAPHY
│   └── Used by: Text elements for font sizes/weights
├── COLORS
│   └── Used by: All components for consistent color palette
├── BORDER_RADIUS
│   └── Used by: Buttons, cards, containers
├── SHADOWS
│   └── Used by: Elevated elements
└── Z_INDEX
    └── Used by: Modals, overlays, dropdowns

All components import and use these tokens
instead of hardcoded values
```

---

## 🔗 Import Patterns

```typescript
// Centralized Imports (Recommended)
import { EmptyState, ErrorState, EmptyProducts } from '@/components/common/states';
import { BottomSheet, SafeAreaContainer } from '@/components/common/mobile';
import { useResponsiveGrid } from '@/hooks';

// Direct Imports (Also works)
import EmptyState from '@/components/common/EmptyState';
import ErrorState from '@/components/common/ErrorState';
import BottomSheet from '@/components/common/BottomSheet';
```

---

## 📊 Component Interaction Map

```
┌────────────────────────────────────────────────────────────────┐
│                      User Interaction                          │
└────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────┐
│                    MainStorePage (Parent)                      │
│                                                                │
│  State:                                                        │
│  ├─ loading: boolean                                           │
│  ├─ error: Error | null                                        │
│  ├─ products: Product[]                                        │
│  └─ filters: FilterState                                       │
│                                                                │
│  Renders:                                                      │
│  ├─ if (loading) → LoadingState                                │
│  ├─ if (error) → ErrorState                                    │
│  ├─ if (empty) → EmptyProducts                                 │
│  └─ else → ResponsiveProductGrid                               │
│                                                                │
│  User Actions:                                                 │
│  ├─ onRetry() → refetch data                                   │
│  ├─ onClearFilters() → reset filters                           │
│  ├─ onOpenFilters() → show BottomSheet                         │
│  └─ onProductClick() → navigate to product                     │
└────────────────────────────────────────────────────────────────┘
```

---

## ✨ Feature Matrix

| Component | Accessibility | Responsive | Animated | Themed |
|-----------|--------------|------------|----------|---------|
| EmptyState | ✅ | ✅ | ❌ | ✅ |
| ErrorState | ✅ | ✅ | ❌ | ✅ |
| EmptyProducts | ✅ | ✅ | ❌ | ✅ |
| BottomSheet | ✅ | ✅ | ✅ | ✅ |
| SafeAreaContainer | ✅ | ✅ | ❌ | ✅ |
| ResponsiveProductGrid | ✅ | ✅ | ❌ | ✅ |

---

## 🎯 Quick Decision Tree

```
Need to show when data is missing?
├─ Generic → Use EmptyState
├─ Products → Use EmptyProducts
└─ Custom → Create new specialized component

Need to show an error?
└─ Use ErrorState (works for all errors)

Need a mobile modal?
├─ Filters → Use BottomSheet
├─ Options → Use BottomSheet
└─ Full screen → Use regular Modal

Need safe areas?
└─ Use SafeAreaContainer

Need responsive grid?
├─ Products → Use ResponsiveProductGrid
└─ Custom → Use useResponsiveGrid hook
```

---

## 📈 Performance Metrics

```
ResponsiveProductGrid Optimizations:

┌─────────────────────────────────────────┐
│ Setting                │ Value          │
├─────────────────────────────────────────┤
│ removeClippedSubviews  │ true           │
│ maxToRenderPerBatch    │ 10             │
│ windowSize             │ 10             │
│ initialNumToRender     │ 8              │
│ Animation Driver       │ Native (60fps) │
└─────────────────────────────────────────┘

Expected Performance:
- 60 FPS scrolling
- < 100ms initial render
- Minimal memory usage
- Smooth animations
```

---

**Created**: Phase 3.3 Implementation
**Status**: Complete and Ready for Integration ✅
